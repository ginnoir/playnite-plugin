<UserControl x:Class="RomM.SaveSync.ConflictResolutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="560" MaxWidth="640">
    <StackPanel Margin="20">
        <TextBlock Text="Save conflict" FontSize="18" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock x:Name="HeaderText" TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="This save changed both locally and on RomM since the last sync. Choose which to keep." />

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="#80808080" Padding="12" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Local (this PC)" FontWeight="Bold" Margin="0,0,0,6" />
                    <TextBlock x:Name="LocalSizeText" />
                    <TextBlock x:Name="LocalTimeText" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" BorderThickness="1" BorderBrush="#80808080" Padding="12" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="RomM (server)" FontWeight="Bold" Margin="0,0,0,6" />
                    <TextBlock x:Name="RemoteSizeText" />
                    <TextBlock x:Name="RemoteTimeText" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="KeepLocalButton" Content="Keep Local" MinWidth="100" Margin="0,0,8,0" Click="KeepLocal_Click" />
            <Button x:Name="KeepRemoteButton" Content="Keep RomM" MinWidth="100" Margin="0,0,8,0" Click="KeepRemote_Click" />
            <Button x:Name="KeepBothButton" Content="Keep Both" MinWidth="100" Margin="0,0,8,0" Click="KeepBoth_Click"
                    ToolTip="Upload the local save to a timestamped slot and pull the RomM save as the live one. Nothing is lost." />
            <Button x:Name="SkipButton" Content="Skip" MinWidth="80" Click="Skip_Click" />
        </StackPanel>
    </StackPanel>
</UserControl>
